{
  "name": "tienda-fullstack",
  "private": true,
  "scripts": {
    "dev": "concurrently -n mailpit,backend,frontend \"mailpit\" \"cd backend && php artisan serve\" \"cd frontend && npm run dev\"",
    "backend": "cd backend && php artisan serve",
    "frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
